# ClassInstantiationWithPrivateConstructor
This project demonstrates how a class having a private constructor is being instantiated(object is created) from another 
class residing in a different package.
